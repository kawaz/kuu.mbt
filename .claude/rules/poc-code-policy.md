# PoC コードの取り扱い

poc/ 以下のコードは、読む必要と目的がない限り読まない。

理由: 古い設計段階の実装がコンテキストに混入し、最新設計との齟齬を生む。
PoC の成果は設計ドキュメントに反映済みであり、コードを直接参照する必要はない。

## 例外

- **poc/poc4/**: Parser struct + getter 方式の参照実装。ResultMap 実装時に直接参照してよい。
