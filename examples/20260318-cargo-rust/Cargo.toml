[package]
name = "cargo-kuu-example"
version = "0.1.0"
edition = "2024"
description = "Cargo CLI argument parsing demo using kuu WASM bridge"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
