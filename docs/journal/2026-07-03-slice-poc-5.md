# 2026-07-03 垂直スライス PoC 第 5 弾 — 効果記述子 / from_entries / flatten 命名 / optional 糖衣 / max unfold

第 5 弾は直近セッションで確定した決定群 (DR-043/044/045) を「動くコード + 契約を輪郭ごと
固定したテスト」で実測する。第 4 弾 66 test を維持しつつ 22 test 追加 = **88/88 pass**。
場所: `poc/` (MoonBit, `kawaz/slicepoc`)。製品コードではなく検証装置。

新規 phase: phase13 (効果記述子, 7)、phase14 (flatten 命名 / max / optional, 8)、
phase15 (from_entries, 4)、phase16 (oracle stress / separator, 3)。

## 追加した AtomicAST 語彙

- `EffectOp` (value.mbt): `Set / Default / Unset / Empty` の効果 op。`committed(op)` は
  `op != Unset` の純関数 (別フィールドで持たない、DR-045 §2)。
- `Binding.op` (value.mbt): 全 binding が (実体, op, operand=value, source, scope, 順序) に
  一様化。`derive(Eq)` の dedup がそのまま DR-038 の効果列同一性 = op/operand を含む精密化。
- `Node::EffMark(name, op)` (node.mbt): 0 トークンの非 set 効果束縛 (variant の default/unset/empty)。
- `Node::BoundedTail(head, budget, lazy)` (node.mbt): max 有限 repeat の porous 尾部。
- `Node::SepArg(name, sep)` (node.mbt): DR-034 separator 分割の値プリミティブ。
- `Entity.accum : String?` (旧 `accumulate : Bool`): accumulators registry (DR-036) の名前。
  組み込み "flatten" (repeat) / "append" (multiple/separator)、None = scalar。
- `RepeatSpec.max : Int?` / `ElemDef.optional` / `ElemDef.variants` / `ElemDef.separator`。
- `FromEntries` enum + `from_entries()` (result.mbt): 旧 `to_map` を 3 用法正規形へ置換。

---

## Task 1: 効果記述子 (DR-045) — phase13, 7 test

値セル操作を純データ `{op, operand?}` で表現。全 binding に `op` を載せ、効果列を一様化した。

### 実測 (契約割れなし)

- **committed の派生**: `committed(Set/Default/Empty)=true`、`committed(Unset)=false` を直接固定。
  別フィールドでなく op の純関数、という DR-045 §2 の主張を実装で確定。
- **op が同一性成分 (DR-038 精密化)**: 同一セル `x` に同一値 `false` を書く 2 エントリで、
  片方 `Set`・片方 `Unset` にすると **ambiguous:2** (効果が違う)。両方 `Set` なら 1 本合流。
  op を binding に載せた瞬間、dedup (`Array[Binding]` の構造比較) が op を含む効果列同一性に
  なった — 第 3 弾で無自覚に「エントリ情報を binding に残さない」設計にしていたのが、DR-045 の
  「op/operand を含む形に精密化」にそのまま乗った。
- **unset がラダーを開く / default がロックする**: color に env 席 (COLOR) + default "auto"。
  `--no-color` (unset) → 未 committed → ラダー後段の env が勝つ (`always@env`)。
  `--color red` (set) → committed → `red@cli`。`--color-default` (default) → committed だが
  値は default、**env を無視してロック** (`auto@default`)。引数なし → env (`always@env`、
  unset と同じ = 「触っていない」)。unset と default の唯一の差 (committed) がラダー挙動を
  分ける、を end-to-end で実測。
- **empty がコレクションを空にする**: `--tag a --tag b --clear-tags` (empty) → `{tag:[]}`。
  `--tag a --clear-tags --tag c` → `{tag:[c]}` (clear 後に再蓄積)。result builder の accumulate
  fold を op 順で処理し、Empty で list をリセットする形に落ちた。
- **variant lowering `{exact, link, effect}`**: `--no-ssl` (ssl の unset variant) が
  `Seq([Exact("--no-ssl"), EffMark("ssl", Unset)])` として greedy 面に落ちる (link は name 経由)。
  set variant (`--gzip` → compress="gzip") は Bind (set 縮退形) に落ちる。

### 契約の所見 / flag (メインへ)

**LOWERING §B.1 は variant の効果 lowering 形を示していない**。DR-045 §由来 が自ら
「variant の非 set effect の lowering が未定義という gap (LOWERING カタログ G-4/G-5)」と
明記している通り。PoC はこの lowering を `{exact, link (by name), effect (op)}` で確立した。
DR-045 §1 の例 (`{"exact":"--no-color","link":"color","effect":{"op":"unset"}}`) と一致。
**LOWERING §B.1 に variant 効果形 (B.1 の long installer 節への追記) を足すべき** — 新たな
矛盾ではなく DR が予告済みの gap を埋めた形。

---

## Task 2: from_entries 3 用法 (DR-044) — phase15, 4 test

第 4 弾の `to_map(RVal, key_from)` 仮実装を `from_entries(RVal, FromEntries)` 正規形に置換。

### 実測 (契約割れなし)

- **(a) `from_entries()`**: 2 要素 seq 配列 `[[a,1],[b,2]]` → `{a:1,b:2}`。
- **(b) `from_entries({key,value})`**: 指名 2 フィールド `[{k:x,v:10},...]` → `{x:10,y:20}`。
- **(c) `from_entries({key})`**: key 昇格・除去、残り全体が値 `[{path,w},...]` → `{/u1:{w:1},...}`。
  第 4 弾の key_from 仮実装 = この (c) だった。authsock map 形 (phase12 test 4) を (c) へ置換、
  全段 "path" で同一結果。
- **入れ子各段の独立選択**: 外側が `path`・内側 `conns` が `id` を持つ 2 段構造で、
  `KeyPromote("path")` は外側だけ map (内側は配列のまま)、`KeyPromote("id")` は外側配列のまま
  内側だけ map。「各段が独立に配列/map を選べる」(DR-044 §2、from_entries は反復グループごとに
  付与) を、各段が持つ key の有無で選択される形で実測。

---

## Task 3: flatten accumulator の正式化 (DR-043/036) — phase14 の 1 test

`Entity.accumulate : Bool` → `accum : String?` に変更。repeat は組み込み名 **"flatten"**、
multiple/separator は **"append"** を accumulators registry (DR-036) から install。両者とも
観測上は list に畳むが、AST が「どの registry accumulator か」を自己記述するようになった。

### 実測

`entity_accum` で `tag`(multiple)="append"、`src`(repeat)="flatten" を固定。fold 自体は観測
不変 (命名を足しただけ)。

---

## Task 4: `optional: true` = `repeat: {min:0, max:1}` 糖衣 (DR-043) — phase14 の 2 test

`ElemDef.optional` を追加、`elem_repeat(e)` で `{min:0, max:Some(1)}` に還元 (独立概念でない)。

### 実測

- **糖衣が閉包に同一還元**: `optional=true` の def と `repeat=rep(0,max=1)` の def が
  **canon_scope 完全一致** + parse も同一。optional は並行パスでなく 1 つの還元、を構造等価で証明。
- **completeness**: optional を使うが RepeatInst を外すと `unknown special vocab 'repeat'` エラー。
  optional は repeat installer の所有語彙 (独立概念でない) を実測で裏付け。

### 契約の所見 / flag (メインへ) — max=1 の結果形

**PoC は「accumulate (list) するのは閉包が ≥2 値を束縛しうる時 (unbounded / max≥2)。max=1
(optional 含む) は scalar セル (0-or-1)」という規則を採った** (`repeat_accumulates`)。理由:
flatten は MULTI cons を畳む機構で、max=1 なら値は高々 1 個なので scalar-or-absent が自然、
かつ optional の直観に一致。

**ただし DR-043 も DR-044 も max=1 の結果形 (list vs scalar) を明記していない**。DR-044 §1 の
「配列が既定」を字義通り取れば optional も 0/1 の配列になりうる。PoC は「≥2 の可能性で list、
max=1 で scalar」という境界規則を**設計判断として持ち込んだ**。DR に明記の余地あり (=
optional/max=1 を scalar とするか 0/1 配列とするか)。

---

## Task 5: min>=2 / max 有限の unfold (LOWERING §B.8) — phase14 の 5 test

`RepeatSpec.max` を追加。unbounded は既存 Ref 再帰、有限 max は `BoundedTail(head, budget, lazy)`。

### 設計判断: BoundedTail を porous 専用ノードに

有限 max を単純な入れ子 Opt (`head + Opt(head + Opt(head))`) で lowering すると、Or 枝が
scope_consume の `other =>` 経由で eval され **先食い (suppressed) を経由しない** + **取り分選好
(greedy/lazy) が効かない** (全 split を枝列挙 = ambiguous 化)。これを避けるため、有限 max は
`BoundedTail(head, budget, lazy)` を spine に置き、`bounded_tail_step` が budget を再帰で減らし
ながら stop/more を porous に (consume_head 経由 = 先食い保持、has_full オラクル = 取り分選好保持)
処理する。budget は spine 再訪でなく再帰深さで減るので固定 spine と両立する。

off-spine (eval が BoundedTail に到達する稀路) 用に `bounded_opt` で入れ子 Opt 等価形も用意。

### 実測 (契約割れなし)

- **min:2 (unbounded)**: `a` fail (min 未達)、`a b` → `[a,b]`、`a b c` → `[a,b,c]`。
- **max 有限 {min:1,max:3}**: `a` → `[a]`、`a b c` → `[a,b,c]`、`a b c d` → fail (4 個目が max 超過
  で消費不能・後続なし)。lowering は `head + BoundedTail(head, 2)` (Ref 再帰なし)。
- **exact {min:2,max:2}**: `a` fail、`a b` → `[a,b]`、`a b c` fail。
- **max × 取り分選好 greedy**: `xs{1,3} ys{1,3}` に `a b c d` → `{xs:[a,b,c],ys:[d]}` (左が最長取り分)。
- **max × 取り分選好 lazy**: xs を lazy に → `{xs:[a],ys:[b,c,d]}` (最短取り分)。
  → **bounded 尾部が Ref path と同じ greedy/lazy 選好を持つ**ことを実測 (取り分選好併用)。

---

## Task 6: 入れ子 repeat の下流バックトラック (取り分完成オラクルのスコープ相対化) — phase16, 1 test

DR-043 末尾が「親の消費が続くスコープ内の repeat では全体長基準の完成判定が壊れる」と明記した
論点を stress。

### 実測 — **DR 予告の限界を具体再現 (contract flag)**

command `seq` の body に greedy repeat `xs`、root に末尾 positional `tail` を置く。
`seq a b c` の正しい解釈は `{seq:{xs:[a,b]}, tail:c}` が存在する。しかし **取り分選好の完成
オラクル `has_full` が大域長 (4) を基準にする**ため、seq 内の greedy `xs` は `[a,b,c]` (大域末端に
到達) にコミットし、root の tail を試す前に `[a,b]` split を**枝刈り**する → tail に残る token が
なく **parse 全体が fail** (`fail:no complete parse`)。

これは DR-043 末尾 (「実装が枝刈りのために局所的な完成判定を使う場合、スコープの消費境界に相対化
しないと近似が壊れる ... 親の消費が続くスコープ内の repeat では不可」) の**具体再現**。第 3 弾の
has_full flag と同根。**新たな矛盾ではなく DR 自身が予告した限界**を concrete case で固定した。
根治 (完成オラクルのスコープ境界相対化 / CPS 化) はフルエンジンの設計論点として持ち越し
(第 3 弾でも「最優先の設計論点」と記録済み)。symptom-fix (テスト側で誤魔化す) はせず、破れる
事実を test として可視化した。

---

## Task 7: repeat × separator 併用 (DR-034/043) — phase16, 2 test

`SepArg(name, sep)` (separator 分割の値プリミティブ) を追加。1 トークン消費 → sep で分割 →
piece ごとに `name=piece` の Set 効果を発する。先食い-aware (consume_head/scope_consume 両方に
case 追加)。

### 実測 (契約割れなし)

- **separator × repeat**: `xs` を repeat + separator "," にして `a,b c,d` → `{xs:[a,b,c,d]}`
  (2 発火 × 2 分割、flatten が全 piece を 1 list に畳む)。`a,b` → `[a,b]`、`solo` → `[solo]`。
- **separator (repeat なし)**: 単一 positional + separator "," で `a,b,c` → `{xs:[a,b,c]}`。
  accum="append" (separator は ≥2 piece を生むので list)。

### 簡略化メモ

separator の所有語彙 completeness は未強制 (DR-034 では multiple pipeline の一部)。テストは全
installer セットで実行。single-char separator のみ (multi-char は射程外)。

---

## ハマり所

- **String indexing は UInt16 を返す** (Char でない)。`sep[0] == char` が型エラー [4014]。
  `for c in s { return c }` 形の `first_char` で先頭 Char を取る形に回避 (starts_dash と同じ流儀)。
- **Node match の網羅性**: EffMark / BoundedTail / SepArg を追加するたび eval / head_progresses /
  scope_consume / consume_head の全 match に case を足す必要 (scope_consume は `other =>` catch-all
  があるので BoundedTail/SepArg は明示 case を先に置かないと 先食い/取り分選好 を素通りする)。
- **Binding への op 追加は全構造体リテラルに波及** (nest / array_marker / mark_scope / nest_index /
  Rooted / strip_link / sub_under)。mkb 経由は自動 Set 化で済むが、リテラル構築箇所は手で追従。
  全て Set 化で既存 66 test は不変 (render は op を出さない)。

## 未解決 / 射程外 (第 5 弾で更新)

1. **完成オラクルのスコープ相対化** (Task 6、最優先の設計論点、第 3 弾から継続)。根治は
   フルエンジンで has_full をスコープ消費境界に相対化 (CPS / スコープ・ローカル・オラクル)。
2. **max=1 の結果形** (Task 4 flag): optional/max=1 を scalar とする PoC 判断を DR に明記するか
   (DR-044「配列が既定」との境界)。
3. **LOWERING §B.1 の variant 効果形追記** (Task 1 flag): DR-045 が予告した G-4/G-5 gap を PoC が
   確立したので、LOWERING に反映。
4. separator の completeness 未強制 / multi-char separator / value 型注釈付き separator
   (`-D KEY=VAL` の 2D、DR-044 射程外) は未実装。
5. link-to-same-cell の複数スコープ同時発火合流 (第 4 弾からの持ち越し) は依然未検証。

---

# 第 6 弾: 一様配列裁定の反映 (DR-044) / 失敗時アクションの候補定義実測 (DR-048)

第 5 弾 flag 2 (max=1 の結果形) に kawaz 裁定「一様配列」が出て DR-044 に明記された。追従して第 5 弾の
「max=1 → scalar」特別化を撤廃。加えて DR-048 (失敗時アクション) の射程外事項「候補経路で selected の
精密定義」の材料集めとして、候補定義 3 案を実装し観測マトリクスを取った。第 5 弾 88 test → **95/95 pass**
(Task 1: optional test を新仕様に書換 + unwrap_single 追加で +1、Task 2: phase17 で +6)。

## Task 1: 一様配列への修正 (DR-044 §1 裁定) — phase14 更新, +1 test

kawaz 裁定: repeat / multiple / separator を宣言した要素は max に依らず**常に配列**。`optional: true`
(= `repeat:{min:0,max:1}` 糖衣) も `[]` / `[x]`。scalar が欲しければ collector (unwrap_single) で畳む。

### 実装

- `ensure_entity` の max ゲート (`repeat_accumulates`) を撤廃。repeat は max に依らず accum "flatten"。
  `repeat_accumulates` 関数は削除 (unused)。
- **空 accumulate セルの `[]` 表示** (一様配列の肝): 発火 0 回の accumulate セルは、第 5 弾までは結果に
  現れなかった (同名 binding が無い → キー欠落)。裁定の `[]` を満たすため result builder を **スコープ
  限定の accum セル注入**に対応させた: `accum_names` (フラット名) → `accum_cells` (result-path 付き)、
  `build_result` に current path を通し、その path に home を持つ accum セルが named に無ければ `[]` を注入。
  path 付与により、command scope 内のセルを top level に誤注入する事故を防ぐ (`serve` 未起動なら
  `serve` scope 自体が結果に現れないので注入も走らない)。
- **unwrap_single collector** (DR-034 縮退, opt-in scalar): 長さ 1 配列を要素へ再帰的に unwrap。
  `[x]` -> `x`、`[]` は unwrap 対象なし -> `[]` のまま。from_entries と同じ RVal->RVal collector 形。

### 実測

- `entity_accum(optional)` が "scalar" -> **"flatten"**。`optional []` -> `{x:[]}`、`optional [a]` -> `{x:[a]}`
  (第 5 弾は `{}` / `{x:a}`)。テストの意図コメントも新仕様 (一様配列) に書き換えた (テスト改変でなく仕様変更)。
- optional と `repeat:{min:0,max:1}` は canon_scope 完全一致を維持 (糖衣還元は不変)。
- `unwrap_single([a])` -> `a`、`unwrap_single([])` -> `[]`。scalar は collector 概念であって構造の特別化で
  ないことを実測。

## Task 2: 失敗時アクションの候補定義実測 (DR-048) — phase17, +6 test

DR-048: early-exit は持たず完走。完全経路 0 本の失敗時、候補経路で失敗時アクション持ち要素 (help) が
selected なら保持 Error に代えて発火。複数は argv 位置最小の先勝ち。射程外 = 「候補経路で selected」の
精密定義 (どの partial 経路を数えるか)。

### 実装

- `Node::FailMark(name)` (0-token、argv 位置を記録)。fail_action フラグの long satellite を
  `Seq([Exact(trigger), Bind(name,true), FailMark(name)])` に lowering。
- `Branch::Held(msg, depth)` -> **`Held(msg, depth, Array[Binding])`** に拡張 = 失敗時部分状態を保持
  (**F-039 との接続**)。number positional の parse 失敗など scope acc が生きている Held 生成点で acc を
  載せる (help marker が dead end でも捕捉できる)。matcher / leaf の Held は `[]`。
- `Outcome::FireAction(name)` 追加。`parse` に `faildef~ : FailDef` を追加。action marker は
  `@act:<name>` キー (renderer / build_result は skip)。argv 位置は marker の value (VNum)。
- **FailDef 3 案**:
  - `AcceptsOnly` … 候補 = scope-end Accept のみ (dead end 無視)
  - `WithHeld` … + Held 到達経路の partial state (F-039)
  - `DeepestOnly` … 最大 argv 消費の候補経路のみ

### 観測マトリクス (phase17)

| 失敗形 | AcceptsOnly | WithHeld | DeepestOnly | 主眼 |
|---|---|---|---|---|
| `--help --typo` (help 先行) | action:help | action:help | action:help | 到達可能な partial Accept に help -> 全案発火 |
| `--typo --help` (失敗先行) | fail | fail | fail | help 到達不能 (positional 無で pos0 詰み) -> argv 順の効果 |
| `-- --help x` (dd 内部) | fail | fail | fail | 先食い/内部一体で --help は raw 消費、help 未発火 |
| `--help x y` (dd 前) | — | action:help | — | 対照: 先食いが効く位置なら発火 |
| `--help --version --typo` | — | action:help | — | 複数アクション -> argv 先勝ち (help@1 < version@2) |
| `--version --help --typo` | — | action:version | — | 同上、順序反転 |
| **`--help abc`** (num positional 失敗) | **fail (保持 Error)** | **action:help** | **action:help** | **候補定義の分岐点** |
| `abc --help` (失敗先行) | fail | fail | fail | abc が先に数値失敗、help 未到達 |

### 観測から得た知見 (issue への材料)

1. **主要な分岐は AcceptsOnly vs (WithHeld / DeepestOnly)**。`--help abc` で help は number positional の
   Held に dead end する。AcceptsOnly は scope-end Accept のみ数えるので help を見落とし保持 Error を出す。
   WithHeld / DeepestOnly は Held の partial state (F-039) から help を拾い発火する。
2. **WithHeld と DeepestOnly は実質一致する** (E で b==c、F で固定)。理由: 先食いが `--help` の raw 消費を
   禁じるので、`--help` を消費した経路は**必ず** help satellite を撃つ。ゆえに「help を撃たずに深く進む
   候補」が存在せず、「最深候補のみ」と「Held 含む全候補」で差が出ない。**-> 精密定義の実質的な選択は
   「dead end (Held の partial state) を候補に含めるか否か」の 1 点に縮約される** (WithHeld を採るか
   AcceptsOnly を採るか)。DR-048 §2 の狙い (`--help --typo` で古典 exit 相当) は AcceptsOnly でも満たせるが、
   `--help abc` のような「help 後に別要素が即エラー」で古典 CLI が help を出す挙動を再現するには WithHeld が要る。
3. **argv 先勝ちは効果列 (DR-045) の argv 順で素直に実装できた** (marker に位置を載せ min を取るだけ)。
4. **dd 抑制は新規則ゼロで導出** (help satellite が severed 面で発火しない = 既存の 先食い/内部一体則)。

### 契約割れ / flag (メインへ)

- **新規矛盾はゼロ**。DR-048 が射程外とした精密定義について、上記知見 2 (WithHeld と DeepestOnly の実質一致、
  分岐は「Held を候補に含めるか」に縮約) を issue `2026-07-03-failure-action-selected-scope` への実測材料
  として提供する。**推奨は WithHeld** (古典 CLI の「help 後に即エラーでも help を出す」を再現でき、かつ
  F-039 の失敗時部分状態と自然に接続する)。ただし部外者観測につき当事者の裏取りを要する。

## ハマり所

- **Held 拡張の波及**: `Branch::Held` に partial state を足すと eval.mbt / matcher.mbt の全 Held 生成・
  伝播点 (計 22) の追従が必要。伝播 `Held(m,d) => out.push(Held(m,d))` は replace_all で一括、leaf の
  Held は文脈ごとに acc or `[]` を手で選択 (positional number 失敗のみ acc を載せれば help 捕捉に足りる)。
- **`FireAction` 追加で Outcome の match が非網羅化**: helper 4 箇所 (effects_str / resolve_after_parse /
  result_str / result_rval) の追従漏れをコンパイラ [0011] が捕捉。

## 未解決 / 射程外 (第 6 弾で更新)

1. 失敗時アクション属性のフィールド名・installer 区分 (DR-048 §射程外、§13.9 未予約) は未確定。PoC は
   `fail_action` bool + long satellite への FailMark 注入で最小実装 (short 経由の fail_action は未対応)。
2. multiple 発火 0 回の `[]` (Task 1 injection は対応済みだが専用テストは optional のみ)。
3. 第 5 弾からの持ち越し (完成オラクルのスコープ相対化、link-to-same-cell 同時発火) は依然未検証。

---

# 第 7 弾: ambiguous 非発火の輪郭固定 (DR-048 確定) / link 合流の別トークン同時発火 (DR-042)

小粒 2 件。いずれも**新規コード変更を要さない検証**で、既存機構が確定仕様を満たすことをテストで固定した
(helper 2 個追加のみ)。第 6 弾 95 test → **98/98 pass** (phase18, +3)。DR-048 は確定形 (WithHeld 採用 +
ambiguous 非発火 + 誘導行 2 段構え) になった。

## タスク 1: ambiguous 非発火の輪郭固定 (DR-048 §2) — phase18, +1 test

DR-048 確定文言「発火条件は完全経路 0 本のみ、2+ 本 (ambiguous) では発火しない」。第 6 弾の `parse` は
FireAction 判定を `full.length()==0` アームにのみ置いていた (ambiguous = `n` アームは Ambiguous を返す)
ので、**確定仕様を既に満たしていた**。輪郭をテストで固定:

- help (fail_action) + 同一トリガ `--v` が別実体 a/b を束縛する 2 エントリ。`--help --v` は help 発火後
  --v で 2 完全経路 ({help,a}/{help,b}) に分岐 = **ambiguous:2**。help は両経路で selected だが発火しない。
- 対照: 同じ定義で `--help --x` (--x 未定義で消費不能) → 完全経路 0 本 → **action:help**。
- 判断本質をコメントに明記: ambiguous の本体は競合解釈の提示、0 本失敗 (使い方不明→help 有益) と
  ambiguous (どの解釈か明確化したい→エラーが本体) では見せたい情報が違う (DR-048)。
- 誘導行 (0 本 + ambiguous 両方の末尾 1 行) はレンダリング層で PoC 射程外 (テストせず)。

## タスク 2: link 合流の別トークン同時発火 (DR-042 検証マトリクス) — phase18, +2 test

第 4 弾が「同一トークン両発火の合流」を確認済み (phase11)。持ち越しの「別トークン同時発火」を実測。
**既存の link 機構 (同名実体への合流 / global link-sync の Rooted) がそのまま多トークン合流を処理する**
ことを確認した (コード変更なし)。ambiguous を誘発しない正常系 (各 1 完全経路)。

### 2a: long / short が同一セルへ別トークンで発火

`port` の long=`--port` と short=`-p` は同名実体セルへ合流する。`--port 80 -p 90` は両入口が別トークンで
発火し、効果 [port=80, port=90] が 1 セルへ積まれる:

- **効果列順序保存 (DR-045)**: `ordered_effects` (消費順、非ソート) が `port=80,port=90` (argv 順)。
- **あと勝ち (DR-015)**: scalar なので結果は最後の効果 = `{port:90}`。
- 順序反転 `-p 90 --port 80` → 効果列 `port=90,port=80`、結果 `{port:80}` (効果列順序が最終値を決める)。

### 2b: global の root 入口 + command 内コピーが別トークンで発火

`out` は global。root の入口と build command 内へ link コピーされた入口 (Rooted) の 2 つが、同一 argv の
別トークンで発火し、両方 root の out セルへ link 同期する:

- `--out A build --out B` → root で out=A、build 内で out=B が root セルへ合流 → あと勝ち `{out:B,build:{}}`。
- 順序反転 `--out B build --out A` → `{out:A,build:{}}` (効果列順序が効く)。
- **複数スコープからの link 合流**を実測 = DR-042 検証マトリクスの当該項目を消化。第 4 弾は同一トークン
  両発火 (phase11 の Rooted 手組み) だったが、本弾は global installer 経由の実配線で別トークン合流を確認。

## 契約割れ / flag (メインへ)

- **新規矛盾ゼロ**。両タスクとも既存機構が確定仕様を満たすことの確認。ambiguous 非発火は `parse` の
  arm 配置で既に成立、link 合流は同名実体 / Rooted link-sync で既に成立。コード変更なし。

## 未解決 / 射程外 (第 7 弾で更新)

1. 誘導行 (`Try 'prog --help' ...` 相当、0 本 + ambiguous 両方) はレンダリング層で PoC 射程外 (DR-048)。
2. 失敗時アクション属性のフィールド名・installer 区分 (DR-048 §射程外) は依然未確定。
3. 第 5 弾からの持ち越し (完成オラクルのスコープ相対化) は依然未検証。

---

# 第 8 弾: 3 段以上の中間 shadowing の name-vs-trigger 微差 (DR-042 検証マトリクス最終独立項目)

DR-042 global installer の shadowing 規則を 3 段木で実測し、**DR-042 文言が読み A / B の両方を許す曖昧さ**を
検出した。実測に加え文言精密化の材料提供が目的。コード変更なし (観測のみ、phase19 +3 test)。第 7 弾 98
test → **101/101 pass**。

## 設定

root(global `verbose`, トリガ `--verbose`) → mid(中間宣言) → sub(無宣言) の 3 段 command 木。mid の宣言を
3 通りに変え、`mid sub --verbose`(孫スコープ発火)と `mid --verbose sub`(中間スコープ発火)で束縛先を観測。

## 観測マトリクス (phase19)

| ケース | mid の宣言 | `mid sub --verbose` | `mid --verbose sub` | 意味 |
|---|---|---|---|---|
| 1 | 別 name・同 literal (chatty=--verbose, 非global) | `{mid:{chatty:true,sub:{}}}` | 同左 | root コピーは sub に届かず、mid の chatty が拾う = **読み B** |
| 2 | 同 name・同 literal (verbose=--verbose, 非global) | `{mid:{verbose:true,sub:{}}}` | 同左 | 通常 shadow の 3 段版、mid の verbose が拾う |
| 3 | 同 name・別 literal (verbose=--chatty, 非global) | `{verbose:true,mid:{sub:{}}}` | 同左 | トリガ非衝突 → root コピーが sub まで伝播 (Rooted link → root キー) |

補足: ケース 3 の mid own トリガ `mid --chatty sub` → `{mid:{verbose:true,sub:{}}}` (mid の別セル)。

## 判明した事実

1. **PoC は読み B (伝播断ち) を実装**。root コピーは is_global を保って level-by-level に伝播するが、mid で
   トリガ衝突により止まったコピーは mid の decl に landing しないため、再伝播元にならない → sub に届かない。
   孫の `--verbose` は sub が verbose 入口を持たないため sub 閉じ→mid 再開 (早閉じ抑制) で mid の chatty に
   読まれる (最内 reader が勝つ)。ケース 1 が読み A なら sub は root コピー入口を持ち root verbose セルへ
   link したはずで、結果は `{verbose:true,mid:{sub:{}}}` になる — 実測は `{mid:{chatty:true,sub:{}}}` なので B。
2. **shadow はトリガ literal 判定 (名前非依存)**。ケース 1 (別 name) でも shadow が成立 (mid が止める)、
   ケース 3 (同 name・別 trigger) では shadow せず伝播 = DR-042 の「判定は要素名ではなくトリガ literal」を
   3 段で追認。名前が重なるだけでは shadow しない。

## 契約割れ / flag (最優先) — DR-042 文言が読み A / B の両方を許す

DR-042 global installer の記述: 「子孫 command スコープへ ref/link 衛星の宣言的コピーを追加する。同一トリガ
literal を自前で持つスコープへはコピーしない (shadowing、最小スコープ優先 = lexical 解決のパース時適用)」。

この 1 文の中に**互いに逆を示唆する 2 つの語**が同居している:

- **「子孫スコープへコピー / 自前で持つスコープには入れない」= per-scope フィルタ**として読むと **読み A**:
  sub は `--verbose` を自前宣言していないので、root コピーを受け取るべき (中間 mid の shadow は mid 自身の
  可否にしか効かない)。
- **「最小スコープ優先 = lexical 解決」= subtree 被覆**として読むと **読み B**: lexical scoping では最内宣言が
  配下全体を shadow するので、mid の `--verbose` 宣言が mid の subtree (sub 含む) 全域で `--verbose` を覆い、
  sub の `--verbose` は mid の chatty に解決される。

**PoC は読み B を実装** (lexical 解決と整合、level-by-level 伝播の帰結)。読み B が意味論的に妥当と考えられる
(DR-032/033 の lexical scope と一貫) が、**コピー機構の記述 (per-scope フィルタ) を字義通り読むと読み A に取れる**
ため、文言だけでは 3 段の中間 shadow の挙動が一意に定まらない。

### 精密化の提案 (材料、仕様確定は当事者に委ねる)

DR-042 に「**中間スコープが同一トリガを shadow した場合、その shadow は配下 command 全体に及ぶ (lexical 連鎖) —
孫が自前宣言をしていなくても root のコピーは伝播しない**」を明記すると、per-scope フィルタ読み (A) を排除して
読み B に一意化できる。逆に読み A を意図する場合は「各子孫スコープは独立に、自スコープの宣言のみで可否判定
する (中間の shadow は伝播を断たない)」と明記する必要がある。どちらを canonical にするかは当事者判断。
部外者観測につき、lexical 意味論 (DR-032/033) との整合確認を推奨。

## 未解決 / 射程外 (第 8 弾で更新)

1. DR-042 3 段 shadowing の読み A/B 一意化 (上記 flag)。仕様側の期待は当事者確定。
2. 第 5 弾からの持ち越し (完成オラクルのスコープ相対化) は依然未検証。

---

# 第 9 弾: config ファイル値源 (DR-050) の垂直スライス + env × config_key

DR-050 (config ファイル値源) を最小 PoC で実装し、DR-042 検証マトリクスの「env × config_key (値源 2 つ)」
を含む検証項目を実測。第 8 弾 101 test → **110/110 pass** (phase20 +9)。ハード契約割れゼロ、型不一致境界の
軽微な flag 1 件。

## 追加した語彙・機構

- `Source::Config` (値源ラダー #3、env と inherit の間)。
- `ConfigVal` enum (CStr/CNum/CBool/CArr/CObj) = JSON 同型の階層オブジェクト (DR-050 §2、provider の返り値)。
- `config_to_value(cv, ty)` = DR-050 §4 の型変換 (string→lenient parse / typed→型一致検証 / mismatch→Error)。
- `Entity`: `config_seat` / `config_key` / `is_config_file`。`ElemDef`: `is_config_file` / `config_key`。
- `ConfigInst` installer (席宣言型、不変則④): owned_vocab "config"、apply_node は no-op (席は ensure_entity で
  決定的にセット、env/inherit と同型)。completeness (config_file / config_key ∉ 所有集合 → error)。
- `has_config_file(def)` (同型 default の発火条件)、`config_cycle_error(def)` (循環禁止の静的検査)。
- `resolve_scope_config(sc, cli, env, provider, path~, inherited~)` = DR-050 §5 の完走後パイプライン
  (config_file 値確定 → provider 読込 → config 席有効化 → 最終値確定)。`config_lookup(obj, path)` で
  絶対パス navigate。返り値は `Result[Array[Binding], String]` (型不一致・読込失敗が Err)。

## 実測 (検証項目ごと)

### env × config_key ラダー (DR-042 マトリクス本命) — 整合

port に env(PORT) + config_key(["port"]) + default を宣言、config_file 要素 `config` が config obj を供給。
4 値源が順に port を供給し source タグ保存: **cli=9090@cli / env=8080@env / config=3000@config / (config
欠落)=1@default**。env > config のラダー順、source:"config" (F-032 の確定) を実測。EnvInst と ConfigInst の
席宣言は順列一致 (canon_scope 同一) = 不変則④の順序非依存。

### 値の型 (DR-050 §4) — 整合

- string → VStr、number(CNum) → VNum (直接)。
- **JSON string "8080" → number 要素 → parse 受理** (寛容 canonical、DR-040)。
- **型不一致 (bool 要素に CNum) → Error** (`err:config number for a bool element (type mismatch)`)。
- **array(CArr) → multiple のpieces** (`tags:[a,b]` → tag=a@config, tag=b@config)。
- **object(CObj) → config_key 絶対パス navigate** (`server.port` → port=9000@config)。

### 同型 default (DR-050 §3) — 整合

config_key 無しの要素は 同型パス (scope path + name、root なら ["port"]) で config を引く。config が別階層
(server.port) にあると 同型パス ["port"] では見つからず default に落ちる = 「結果と同じ形」対応を実測。

### パス解決ラダー (--config > env > default) — 整合

config_file 要素のパス自体が cli > env > default で解決。path 別に別 obj を返す provider で確認:
cli=/cli.toml→11 / env=/env.toml→22 / default=/default.toml→33、いずれも @config。

### null (読込失敗) — 整合

committed パス (CLI 明示) + provider None → **Error** (`config file "/missing.toml" could not be read`)。
default パス + provider None → **黙認** (config 席空、port は default 7 に落ちる)。

### 循環禁止 (DR-050 §5) — 整合

config_file 要素自身に config_key → 静的エラー (`config_file element 'config' cannot have a config_key (循環禁止)`)。

### config は構造に影響しない (DR-050 §5 不変条件) — 整合

`--port 80` の parse 結果は config の有無に依らず `ok{port=80}` (config は resolve フェーズのみ、matcher/
経路探索に不関与)。port は config 席あり、config_file 要素は config 席なし (循環禁止) を実測で確認。

## 契約割れ / flag (メインへ)

- **ハード契約割れゼロ**。config installer は DR-050 の構造 (席宣言型・完走後パイプライン・循環禁止・
  同型/config_key) を忠実に実装できた。

### 軽微 flag: DR-050 §4 の非 string 型不一致の境界が未明記

DR-050 §4 は寛容方向 (CStr → 任意型へ parse) を明示し、不一致例を 1 つ (「bool 要素に number → Error」)
挙げるが、**その他の非 string 不一致の扱いを網羅していない**。PoC は「型一致検証」を厳格に読み、以下を全て
Error とした:

- CNum → TStr (number を string 要素へ)
- CBool → TNum / TStr
- CArr → scalar 要素 / CObj → scalar 要素

**特に CNum → TStr は「数値を表示同様に文字列化して受理」も設計としてありえる** (JSON の number を string
要素が受ける寛容化)。PoC は厳格 (Error) を選んだが、DR-050 は明記していない。DR-050 §4 に非 string 値の
mismatch の網羅規則 (どこまで厳格、どこから寛容) を明記すると一意化できる。提案は材料、確定は当事者判断。

## 未実施 / 射程外 (第 9 弾で更新)

1. **command スコープの 同型 default** (serve.port を path=["serve"] で引く): resolve_scope_config は path
   引数で対応済みだが、テストは root 同型のみ (command 同型は機構あり・未テスト)。
2. 複数 config_file 要素の合成 / config watch / プロファイル切替 — DR-050 §射程外。
3. 非 string 型不一致の境界確定 (上記 flag)。
4. 第 5 弾からの持ち越し (完成オラクルのスコープ相対化) は依然未検証。

---

# 第 10 弾: config 型変換を寛容の双方向対称へ修正 (DR-050 §4 裁定 a)

第 9 弾 flag に kawaz 裁定 a (寛容の双方向対称) が出て DR-050 §4 が網羅規則に更新された。追従して
`config_to_value` を修正。第 9 弾 110 test → **111/111 pass** (phase20 §4 test 書換 + flag test +1)。

## 変更

`config_to_value(cv, ty)` の非 string 分岐を裁定 a に修正:

- **CNum → TStr / CBool → TStr: JSON 文字列化で受理** (`1.5` → "1.5"、`true` → "true")。第 9 弾は Error
  だった。根拠: string 要素は CLI/env で「何でも受ける」型なので config でも同じ観測 (引用符の有無で挙動が
  割れない) + `"8080"` → number の parse 受理との双方向対称。`num_to_string` helper を追加 (整数値は小数点
  なし、非整数は decimal)。
- **CNum → TFlag / CBool → TNum: Error 維持** (意味の飛躍。エラー文言を "no number↔bool coercion" に更新)。
- **CArr / CObj → scalar: Error 維持** (構造不一致。文言を "structural mismatch" に統一 — resolve_entity 側の
  非 accumulate CArr 分岐も同文言に揃えた)。

phase20 §4 test を裁定による仕様変更として意図コメントごと書換 (寛容の双方向対称 / 意味の飛躍・構造不一致は
Error)。双方向の全ケースを固定: matching (str/num/bool)、str→num/bool parse、num/bool→str 文字列化、
bool↔number Error、array→scalar Error。

## 実測 (整合)

- `name`(TStr) に CNum(1.5) → `name=1.5@config`、CBool(true) → `name=true@config` (裁定 a の追加半分)。
- `flag`(TFlag) に CNum(1) → Error、`port`(TNum) に CBool(true) → Error (意味の飛躍)。
- `name`(TStr) に CArr → Error (構造不一致)。
- 既存の str→num/bool parse 受理は不変。

## flag (メインへ): 整数値 double の文字列化が serialize 依存

**数値の文字列化で `1.0` が "1" か "1.0" かが実装で割れる**。JSON の number は単一型で「1」と「1.0」を区別
しないので、`CNum(1.0)` はどちらだったかの記憶を持たない → 文字列化で一方を選ぶしかない。PoC (MoonBit) の
`num_to_string` は**整数値 double を小数点なしで描画** (`1.0` → "1")。一方 JSON serializer によっては "1.0"
を出すものもある。DR-050 §4 は「JSON serialize の標準表現」とだけ書き、整数値 float の `.0` 保持を明記して
いない。非整数 (`1.5` → "1.5") は曖昧さなし。phase20 に `CNum(1.0)` → `name=1@config` を固定するテストを置き、
分岐点を可視化した。DR-050 §4 に整数値 float の描画規則 (最短形 "1" か "1.0" か) を明記すると一意化できる。
提案は材料、確定は当事者判断。

## 未解決 / 射程外 (第 10 弾で更新)

1. 整数値 double の文字列化規則の確定 (上記 flag)。
2. 第 9 弾からの持ち越し (command スコープの 同型 default、複数 config_file) は依然未テスト / 射程外。
3. 第 5 弾からの持ち越し (完成オラクルのスコープ相対化) は依然未検証。

---

# 第 11 弾: 結果の欠落表現 (DR-051 absent) + int 型 / config null (DR-050 §4)

DR-051 (値の無い要素は absent = キーなし、null は値空間に持たない) の輪郭固定と、DR-050 §4 追記 (int 行 /
null 行) の実測。第 10 弾 111 test → **119/119 pass** (phase21 +8)。ハード契約割れゼロ、PoC 未実装の
coverage note 2 件。

## 追加した語彙

- `Ty::TInt` (整数制約付き数値、DESIGN §3.3)。CLI/env/matcher では number 同様に扱い、整数制約は config_to_value
  で強制 (float は number の別名なので TNum で表現)。
- `ConfigVal::CNull` (JSON null)。`is_integer(d)` helper。
- config_to_value に int 行 (整数チェック) と CNull 防御 arm を追加。resolve_entity の config 席で CNull は
  「供給なし」= fall through。

## 実測 (検証項目、全整合)

### absent (DR-051 §1) — 既に PoC の既定挙動

**PoC は既に absent を実現していた**: build_result / resolve_entity は binding の無い要素のキーを結果に出さない
(scalar は binding なし → キーなし)。値源を持たない `name` を未指定で resolve → `` (キーなし、null でも
ゼロ値でもない)。指定すれば present。**DR-051 §1 と既存実装が最初から整合** (result builder は absent を
in-band null でなくキー欠落で扱う)。

### absent にならない 3 族 (DR-051 §2)

- **(a) default 持ち**: `port`(default=5) を未指定 resolve → `port=5@default` (常に present)。
- **(b) 反復系**: `optional`(= repeat{min:0,max:1}) を 0 回 → `{xs:[]}` (第 6 弾の一様配列、absent にならず [])。
- **(c) required (構造的)**: mandatory positional `file` を未指定 parse → `fail:no complete parse` (経路不成立、
  DR-047 値充足)、指定すれば `ok{file=x}` = success には必ず値がある。

### ParserContext メタ保持 (DR-051 §5, 2 層分離)

absent な `name` は結果オブジェクトから省かれるが、entity roster (sc.entities) は `name` の存在を保持
(`entity_exists`=true)。`ctx_report` で absent の `name` は present=false/committed=false、default 由来の
`port` は present=true/committed=false (default は CLI 選択でない) を導出。**結果オブジェクト (absent 省略)
と ParserContext (全要素メタ) の 2 層分離**を実測。

### config JSON null = 供給なし (DR-050 §4 / DR-051 §4)

config `{port: null, name: null}` → port は default 9 に落ち、name (default なし) は absent。null は kuu の
値空間に無く、config 席から流れない。対照: 実値 `{port: 3000}` → `port=3000@config`。

### int 型 (DR-050 §4 int 行 / DESIGN §3.3)

- int 要素に JSON 整数 (42) → 受理、非整数 (1.5) → Error、JSON string "42" → parse 受理、"1.5" → Error。
- **number との対照**: number 要素は 1.5 受理 (`v=1.5@config`)、int 要素は同じ 1.5 を Error。数値 3 種の関係
  (number = 汎用、int = 整数制約、float = number 別名) を実測。

## 契約割れ / flag / note (メインへ)

- **ハード契約割れゼロ**。DR-051 の absent は PoC の既存 result builder が最初から実現 (キー欠落 = absent)、
  DR-050 §4 の int/null も忠実に実装。

### note 1 (PoC 未実装): flag / count の preset default 未実装 → PoC flag は absent になりうる

DR-051 §2 は「flag / count は preset default (false / 0) を同梱するので absent にならない」とする (LOWERING
§A.5)。**PoC は flag preset の default 同梱を実装していない**ため、default 未指定の flag option は未指定時に
absent になる (DR-051 §2 と挙動が割れる)。これは DR の問題でなく **PoC の coverage gap** (flag/count preset
未実装)。3 族のうち flag/count は未テスト、default/repeat/required (構造) で代替検証した。

### note 2 (PoC 射程): int の CLI 側制約は未強制

int の整数制約は config_to_value で強制するが、CLI/env の値解析 (value_prim/matcher/env_value) では int を
number 同様に扱う (CLI `--n 1.5` は PoC では受理)。int の CLI 側 validation は第 11 弾の射程外。

### 第 10 弾 flag の解決確認

第 10 弾の「整数値 double の文字列化が serialize 依存」flag は DR-050 §4 で確定: **最短形 (1.0 → "1") が
唯一の一貫解** (常に .0 だと 8080 → "8080.0" になる、ECMA-404 は整数/浮動小数を区別しない)。PoC の
num_to_string がこの canonical と一致。Python 系 serializer の "1.0" は conformance 比較点として明記された。

## 未解決 / 射程外 (第 11 弾で更新)

1. flag / count preset default (LOWERING §A.5) の PoC 実装 (note 1)。
2. int の CLI 側制約 (note 2)。
3. 第 5 弾からの持ち越し (完成オラクルのスコープ相対化) は依然未検証。

---

# 第 12 弾: 結果キー軸の一本化 (DR-052 export_key) の実測

DR-052 (export bool 廃止、export_key: string | null の 1 フィールド) を実測。第 11 弾 119 test →
**127/127 pass** (phase22 +8)。ハード契約割れゼロ、note 3 件。

## 設計判断: 結果キー remap は AtomicAST の外 (post-parse)

DR-052 は「lexical スコープ (name) と id (ref/link) は export_key と直交で不変」と明記。PoC もこれに従い、
**export_key は AtomicAST / installer / entity に一切触れず、post-parse の結果 binding remap のみ**で実装:

- `ExportKey` enum (EkUnset / EkKey(String) / EkNull)、`ElemDef.export_key` / `CommandDef.export_key`。
- `build_export_map(def)` = name → ExportKey (Definition から直読み、Scope でなく)。
- `apply_export_keys(binds, map)` = 勝ち経路 binding の key / scope label を remap (build_result の前段)。
- `find_export_collision(binds, map)` = 露出キー衝突の実行時検査 (DESIGN §15.5)。

**PoC に旧 export 相当の機構は無い** (結果キーは常に name)。DR-052 は clean な追加で、既存 result builder
との食い違いゼロ (team-lead 懸念の「旧 export 機構」不在)。

## remap の意味論

- 値セルの key: `EkKey(s)` → s (上書き) / `EkNull` → "" (nameless: kv 文脈で build_result が skip = 消滅、
  array 要素文脈では "" scalar として値が残る) / `EkUnset` → name (不変)。
- scope label (command/name スコープ): `EkKey(s)` → s / `EkNull` → **label を drop** (子が昇格) / `EkUnset` → 不変。
- sentinel ("[]"/"{}"/""/"@act:") と index ("#k") は map に無い → 保持。null command の "{}" marker は
  label drop で root へ浮くが sentinel 無視されるので無害 (nameless command はキー無し = 子が昇格)。

## 実測 (8 項目、全整合)

1. **キー上書き**: `export_key:"alias"` → `{alias:80}` (name キー port は出ない)。
2. **透過 (kv)**: option に `EkNull` → 値ごと消える (`{verbose,keep}` → `{keep:true}`)。値の伝搬 (cell/link)
   は不変、キーのみ無くなる。
3. **透過 (seq/array)**: IdxRepeat 要素 `k` に `EkNull` → 各要素が bare 値に (`[{k:a},{k:b}]` → `[a,b]`)。
   配列の形 [.,.] は保存 = seq 文脈は値が残る (kv の消滅と対照)。
4. **昇格露出**: command `serve` に `EkNull` → serve キー無し、port が親へ昇格 (`{port:80}`)。対照: 通常は
   `{serve:{port:80}}`。
5. **"" = null**: `ek("")` = EkNull → 透過 (`{keep:true}`)。
6. **直交性**: `export_key:"portnum"` + long/short 同名 link → `--port 80 -p 90` が 1 セルへ合流 (あと勝ち 90)、
   結果は export_key 下に (`{portnum:90}`)。link/id は parse 時・直交で不変。
7. **presence marker**: 選ばれた空 command → `{serve:{}}` (スコープ生成 = 値発生)、選ばれない → absent (`{}`)。
8. **露出キー衝突**: 2 要素に同じ `export_key:"x"`、両露出 → `collision:x` (実行時 ambiguous、§15.5)、片方のみ
   → `{x:true}` (衝突なし = 実際の同時露出時のみ検出)。

## 契約割れ / flag / note (メインへ)

- **ハード契約割れゼロ**。DR-052 は直交な結果 remap として PoC に clean に載り、旧 export 機構との食い違いなし。

### note 1: item 3 の「from_entries 2 要素 seq」は完全には未モデル

DR-052 §2 の中核 (seq 文脈で値が残る・配列の形が変わらない) は IdxRepeat 単一要素 rep で実測した
(`[{k:v}]` → `[v]`)。ただし **2 つの null 要素で 2 要素配列 `[k,v]` を作る形は PoC の parse 経路では出せない**
(同一 rep スコープの 2 個の "" binding は build_result の "" scalar 処理で 1 値に潰れる)。from_entries の
entries 形は phase15 で RVal を直接構築しており parse 経路を通らないので、下流破壊の有無は「配列の形が保存
される」(実測済み) から従う。2 要素 seq 固有の形は PoC の result builder (name → object) の構造上未モデル。

### note 2: export_key remap は name 一意前提

`build_export_map` は name → ExportKey の平坦 map。同名要素が別スコープにあると conflate する (フル
エンジンでは per-element scoped であるべき)。PoC テストは name 一意で回避。

### note 3: 衝突検査は parse Outcome と別経路

item 8 の露出キー衝突は `find_export_collision` (結果 helper 内) で検査し、parse の Outcome (DR-038 ambiguous)
とは別。DESIGN §15.5 の「露出キー一意性は実行時」を post-result 検査で realize。フルエンジンでは結果構築/
露出検査の一部になる。

## 未解決 / 射程外 (第 12 弾で更新)

1. 2 要素 seq の透過形 (note 1)、export_key の scoped 化 (note 2)。
2. definitions 内要素の露出 opt-in しない (DR-052 §射程外) は PoC も未実装 (template は結果に出ない)。
3. 第 5 弾からの持ち越し (完成オラクルのスコープ相対化) は依然未検証。

---

# 第 13 弾: パース結末の構造 (DR-053) の整備 + 実測

DR-053 (outcome 3 値の discriminated union、errors 全保持 + 最深 primary、ambiguous 全解釈列挙) へ Outcome
を再構築。第 12 弾 127 test → **133/133 pass** (phase23 +6)。ハード契約割れゼロ、note 3 件。既存テストは
render_outcome の後方互換により無改変で移行。

## 再構築

- `Branch::Held(String, Int, Array[Binding])` → **`Held(ParseError, Array[Binding])`**。`ParseError` =
  {element, argv_pos, kind: KParse|KFilter|KConstraint, message}。全 Held 生成点 (eval/matcher) が
  ParseError を積む。
- `Outcome` を 3 値 union に:
  - `Success(Array[Binding])`
  - `Failure(FailureData)` = {errors 全保持, fired_action?, help_entry?, tried_triggers}
  - `Ambiguous(AmbiguousData)` = {interpretations 全列挙, help_entry?}
  - **旧 `Fail(String)` / `FireAction(String)` は `Failure` に統合** (fired_action フィールド)、旧
    `Ambiguous(Int)` は interpretations 付きに。
- `parse` を再構築: 全 Held を errors に dedup 収集、full を interpretations に、action markers →
  fired_action (argv 最小)、`find_help_entry` / `tried_triggers_of` で素材収集。
- **後方互換**: `render_outcome` は Failure → "action:<n>" | "fail:<primary message>" | "fail:no complete
  parse"、Ambiguous → "ambiguous:<count>" を出す。既存 127 test の spec-string が不変 → 無改変で通過。
- kind の filter/constraint 源として最小ノード `FilterArg(name, max)` (>max で KFilter) と `ReqArg(name)`
  (token 無しで KConstraint = required 未充足) を追加 (PoC に real post_filter / DR-047 遅延述語は無いため)。

## 実測 (7 検証項目 → 6 test、全整合)

### errors 全保持 + primary = argv 最深

`Or([Seq([NumArg a]), Seq([StrArg s, NumArg b])])` に `x y`: 経路 A は a が pos0 で parse 失敗、経路 B は s が
x を食い b が pos1 で失敗 → **errors = [a@0/parse, b@1/parse] 全保持**、primary = 最深 (b@1)。

### 同深タイ → 全 primary

`Or([NumArg a, NumArg b])` に `x`: 両者 pos0 で失敗 → **同深は全て primary** (a@0, b@0 両方)。

### kind 3 値

- **parse**: `NumArg` に非数値 → `n@0/parse`。
- **filter**: `FilterArg(port, max=1000)` に `5000` (parse は成功だが max 超過) → `port@0/filter`。
- **constraint**: `ReqArg(file)` に token 無し (required 未充足) → `file@0/constraint`。

### ambiguous interpretations (結果オブジェクト形ビュー)

`-n1.0f` (値付着 n=1 vs 分割 n=1 + f=true) → `ambiguous:2`、各解釈を結果ビューで列挙
`[{n:1,f:true},{n:1}]` (効果列でなく差分が読める形)。

### fired_action と errors 両立 + primary(最深) vs action(最小) 共存

`--help abc` (WithHeld): **fired_action = help** (argv 最小の意図、DR-048)、**errors = [n@1/parse] 残る**、
**primary = n@1** (argv 最深の躓き)。向きが逆 (action 最小 / primary 最深) でも同一 Failure 内で両立 —
表示がアクションに置換されても errors は構造として残る。

### help_entry / tried_triggers

help 入口 (--help) + --verbose + number positional の def で `--typo` 失敗 → **help_entry = "--help"**
(綴り、誘導行の素材)、**tried_triggers = [--help, --verbose]** (試行 exact 群、Did you mean の素材)。

## 契約割れ / note (メインへ)

- **ハード契約割れゼロ**。Outcome 再構築は clean な migration (render_outcome 後方互換で既存無傷)。旧
  FireAction が Failure.fired_action に統合され DR-053 の「fired_action と errors 両立」に自然に一致。

### note 1: tried_triggers は近似 (スコープ全 exact、位置非フィルタ)

DR-053 は tried_triggers = 「失敗位置で試行された exact 綴り」。PoC は `tried_triggers_of` でスコープ greedy
面の全 exact トリガを集める近似 (失敗位置での position-filter はしていない)。DR-053 が明記する通り近接マッチは
DX 層の関心で素材のみ (F-016)、AtomicAST にフックは持たないので、素材として全トリガを載せるのは方向として整合。
position-filter の精密化はフルエンジンの関心。

### note 2: kind filter/constraint は最小 demo ノード

PoC に real post_filter (DR-034) / DR-047 遅延述語は未実装なので、KFilter/KConstraint の源として `FilterArg` /
`ReqArg` を最小追加。エラーは real eval 経路を通り正しい kind を積む (hand-build でない) が、filter/constraint
の完全な subsystem ではない。

### note 3: partial ParserContext (DR-053 §5) は未露出 (§5 optional 予約と整合)

Held は partial state (F-039、fired_action 判定用) を binds で持つが、FailureData には partial_context
フィールドを露出していない。DR-053 §5 は「optional 予約、中身の形は射程外」なので、未露出は仕様と整合。

## 未解決 / 射程外 (第 13 弾で更新)

1. tried_triggers の position-filter 精密化 (note 1)、filter/constraint の完全 subsystem (note 2)。
2. partial ParserContext の中身 (DR-053 §5、F-039 の残り) は未確定。
3. 第 5 弾からの持ち越し (完成オラクルのスコープ相対化) は依然未検証。

---

# 第 14 弾: parse_definition の失敗挙動 (DR-054) の整備 + 実測

DR-054 (定義時検査の Error/warn 境界、2 値 union、全列挙、hint) へ parse_definition を整備。第 13 弾
133 test → **138/138 pass** (phase24 +5、既存 completeness テスト 5 件を新形式へ移行)。ハード契約割れ
ゼロ、循環 ref の区別基準は明快 (flag なし)、note 3 件。

## 再構築

- `parse_definition` の返値を `Result[Scope, String]` → **`Result[Scope, Array[DefError]]`** (DR-054 の
  2 値 union: Ok=success / Err=definition-error)。
- `DefError` = {element, kind: DefErrKind, message, hint}、`DefErrKind` = DVocabIntersection /
  DUnknownVocab / DInvalidRange / DAbsentRef / DCircularRef / DZeroProgress / DConfigCycle。
- 全 static check を **collector 化** (String? first-error → Array[DefError] 全列挙): collect_vocab_
  intersection / collect_unknown_vocab / collect_invalid_range / collect_absent_ref / collect_circular_
  ref / collect_zero_progress / collect_config_cycle。parse_definition が 1 リストに集約 → 非空なら Err。
- hint は全 Error に付与 (DR-042 完全性検査の「次の手」様式を一般化)。
- 既存 completeness テスト (phase2/3/4/14/20) を `Err(errs) => inspect(deferrs_str(errs), ...)` へ移行
  (仕様変更として意図コメント更新)。build_or_fail も `Err(errs)` 追従。

## 循環 ref vs repeat 正当自己 ref の区別 (要注意点、基準は明快)

**基準: ref が HEAD 位置 (消費前に到達可能) にあるサイクルか否か**。これは zero-progress ガードと同一の
左再帰判定原理。3 者が clean に分離する:

- **circular-ref**: `head_ref_of` (head 位置の ref) を辿ってサイクル (`head_cycles`)。例 `bad = Seq([Ref(bad)])`
  → head_ref = Ref(bad) → サイクル → Error。
- **zero-progress**: head が消費しない (head_progresses false) が**サイクルでない** (0-token Bind head)。
  circular と区別するため `!head_progresses && !head_node_cycles` で判定。
- **repeat 正当自己 ref**: `Seq([head, Opt(Ref(self))])` の head は**消費ノード** (StrArg 等) なので
  head_ref_of = None → circular でない、head_progresses = true → zero-progress でもない → 通る。

実測で対照: `bad = Seq([Ref(bad)])` → `bad/circular-ref`、`repeat=rep(1)` の `f` (cons 尾部自己 ref) →
エラーなし・ビルド成功・`{f:[a,b]}` で動作。**区別基準は曖昧にならなかった** (head 位置か否かの一点で決まる)。

## 実測 (7 検証項目、全整合)

1. **全列挙**: 不在 ref (`x`→missing) + zero-progress repeat (`loop`→Bind head) を 1 定義に仕込み、
   `loop/zero-progress,x/absent-ref` の両方が 1 回で返る (first-error 打ち切りなし)。
2. **未知の特殊語彙 + hint**: env:PORT + EnvInst 欠如 → `port/unknown-vocab`、hint「register an installer
   owning 'env:PORT'...」。
3. **不正な値域**: repeat min 3 > max 1 → `f/invalid-range`、hint「set the repeat max >= min」。
4. **不在の ref**: ref_target `nope` がどこにも無い → `x/absent-ref`、hint「define 'nope'...」。
5. **循環 ref + 対照**: 上記の区別セクション参照。
6. **kind 付け替え** (移行テスト): ゼロ進捗 → `loop/zero-progress`、config 循環 → `config/config-cycle`、
   vocab 交差 → `long/vocab-intersection`、completeness → `<elem>/unknown-vocab` が新形式で返る。

## 契約割れ / note (メインへ)

- **ハード契約割れゼロ**。DR-054 の 2 値 union・全列挙・hint を忠実に実装。循環 ref の区別基準は明快で
  flag 不要 (head 位置サイクルの一点で決まる、zero-progress と同一の左再帰原理)。

### note 1: export_key bool の invalid-range は「表現不能」で検査不要

DR-054 §1 は「export_key に bool → invalid-range Error」を挙げるが、PoC の `ExportKey` enum
(EkUnset/EkKey/EkNull) は bool を**表現できない** (DR-052 で export bool は廃止済み)。invalid state が
型で排除されているので検査対象が無い (make invalid states unrepresentable)。UsefulAST の JSON が
`export: true` を書いた場合の弾きは UsefulAST→ElemDef 変換層の関心で、PoC の typed ElemDef の外。

### note 2: 不在 link / template 内部 Ref は未検査

collect_absent_ref は要素の ref_target のみ検査。PoC に定義時の link ターゲット概念は無い (link は
global コピーの is_link フラグ・by-name 解決) ので不在 link は未検査。registry template 内部の Ref
(color テンプレが r を参照等) の不在も未検査 (要素 ref_target で足りるため)。

### note 3: warn 層は PoC 射程外 (指示通り)

DR-054 §1 の warn 群 (露出キー衝突可能性・同一トリガ重複・丸呑み構造) は lint の関心で実行時 bundle
非同梱。指示通り実装せず。

## 未解決 / 射程外 (第 14 弾で更新)

1. warn 層 (lint) / 不在 link / template 内部 Ref 検査 (note 2,3)。
2. 制約間の意味矛盾 (exclusive_group+requires 等) は DR-054 §2 で v1 warn (lint)、PoC 未実装。
3. 第 5 弾からの持ち越し (完成オラクルのスコープ相対化) は依然未検証。

---

# 第 15 弾: 制約語彙の拡充 (DR-055) — constraint installer + 遅延述語 3 種

## やったこと

DR-055 (制約語彙: requires / conflicts_with / exclusive_group + 値依存 requires) を実装 + 実測。
違反 = 完全経路の不成立 = Error `kind:constraint` (DR-047 / DR-053) で統一。constraint は席宣言型
installer (`ConstraintInst`) が回収し、遅延述語として `Scope.constraints` に積む (構造衛星なし)。

### 実装した部品

- **node.mbt**: `Scope` に `constraints: Array[Constraint]` を追加。`Constraint` enum 4 値
  (`CRequires(elem, targets)` / `CRequiresIf(elem, value, targets)` / `CConflicts(elem, targets)` /
  `CExclusive(group, members)`)。
- **installer.mbt**: `ElemDef` に `requires` / `conflicts_with` / `exclusive_group` /
  `value_requires` の 4 フィールド。`ConstraintInst` (owned_vocab = "constraint") が `inst_constraint`
  で各要素の宣言を回収 (add-if-absent、exclusive は group 名でメンバを scope 横断収集)。completeness
  scan は constraint 語彙があって "constraint" が未所有なら `constraint:requires` 等を返す。
- **eval.mbt**: `is_committed` (最終効果が Unset 以外 = last-wins)、`value_present` (committed OR
  default = DR-047 値充足)、`elem_value` (committed 値 last-wins)。`eval_constraints` が完全経路
  (`Accept` で全消費) に到達した束縛列に対し遅延述語を評価し、違反を `ParseError kind:KConstraint` 化。
  parse 統合: 完全経路成立後に `eval_constraints` を走らせ、違反があれば full 候補から外し errors へ。

### 実測 (phase25_wbtest.mbt、6 テストブロック追加、138 → 144 全 green)

1. **installer + completeness + 順列**: requires を書いて `ConstraintInst` 抜きで build →
   `a/unknown-vocab` (DR-054)。`ConstraintInst` を先頭/末尾どちらに置いても `--a` で `a@1/constraint`。
2. **requires (正の依存)**: `--decrypt` 単独 → `decrypt@1/constraint`、`--decrypt --key-file k` → ok、
   `--decrypt` 無 → 空真 (committed のみ発火)。目的語 key_file に default → `--decrypt` 単独で充足
   (値充足は default を数える)。
3. **conflicts_with (対称)**: `--foo --bar` / `--bar --foo` どちらも `foo@2/constraint` (片側宣言で
   両方向)。片方のみ → ok。`--foo --bar --no-foo` は foo の最終効果 Unset → 非 committed → 衝突立たず
   (Success)。
4. **exclusive_group + conflicts_with の重複宣言**: 同一ペア (a,b) を両方に宣言 → 両述語が発火し
   2 エラー `a@2/constraint,g@2/constraint`。正しいが冗長 (冗長指摘は lint、放置が仕様)。
5. **値依存の合成 (F-028 の解)**: `format value_requires json→[schema]` を `CRequiresIf` でモデル化。
   `--format json` 単独 → `format@2/constraint`、`--format yaml` → ok (json 枝のみ活性)、
   `--format json --schema s` → ok。
6. **制約が経路選択に参加 (DR-047 §6 帰結 1)**: `x` が foo+bar と foo 単独の 2 完全経路を持つ Scope +
   `CConflicts("foo",["bar"])` → foo+bar 経路が制約で不成立になり foo 単独に確定 (`ok{foo=true}`、
   ambiguous ではない)。制約なしなら同じ 2 経路が `ambiguous:2`。

## 契約割れ / flag (メインへ)

- **ハード契約割れゼロ**。
- **項目 5「exact 枝の committed」判定に曖昧さは無い** (flag 対象なし)。単一値セルの committed 値は
  一意なので `committed && value==exact` が「その exact 枝が committed」と厳密一致する。値の枝が複数
  exact を持っても、token から決まる committed 値は 1 つなので活性 exact も 1 つ。ambiguous 化しない。

### note: committed vs default の非対称 (DR-047 由来、意図的・曖昧さ無し)

値依存 requires の実装で 1 点の設計判断が要った: **value_requires は TRIGGER なので committed 判定**
(DR-047: exclusive/requires の trigger = committed)、一方 **requires の TARGET 充足は値充足判定**
(default を数える)。帰結として `format` が **default=json** (ユーザ未選択) の場合、値依存 requires は
**発火しない** (trigger は committed のみ、default は passive fallback で非 committed)。この非対称は
DR-047 の trigger=committed / target=値充足 の分割からそのまま導かれる。曖昧ではないが設計判断なので
テストで pin した (`def_dj`: default=json + CLI 無 → `not-failure`)。

## 未解決 / 射程外 (第 15 弾で更新)

1. 制約間の意味矛盾検査 (exclusive_group+requires の論理矛盾等) は DR-054 §2 で v1 warn (lint)、PoC
   未実装。項目 4 の「同一ペアを exclusive+conflicts 二重宣言」も lint 対象 (実行時は両評価が正しい)。
2. warn 層 (lint) / 不在 link / template 内部 Ref は依然 PoC 射程外 (第 14 弾 note 2,3)。
3. 第 5 弾からの持ち越し (完成オラクルのスコープ相対化) は依然未検証。

---

# 第 16 弾: alias (DR-057) — 参照ファミリー 3 人目、name 導出入口の再導出継承

## やったこと

DR-056 (所有 vs 参照) / DR-057 (alias) / DR-058 (hidden/deprecated) + DESIGN §14.5 を読み、alias を
実装 + 実測。alias = 独立要素で書く別入口 (ref/link/alias の 3 人目)。意味論は link + 表示帰属:
効果は canonical 実体セルへ、結果キーは canonical のみ。

### 実装した部品

- **node.mbt**: `DeprMark(String)` node (0-token、DR-058 の deprecation 警告マーカー、FailMark 同型)。
  `ShortEntry` に `depr: Bool` (deprecated alias の cluster entry が警告を出す)。
- **installer.mbt**:
  - `AliasDef` 構造 `{target, name: String?, short, long_override: Array[String]?, deprecated}`。
    `name` None = canonical 名再利用 (short 専用 alias)。`long_override` None = variant 継承 /
    Some(xs) = 明示上書き (Some([]) は variant を切る、DR-007 差分上書き)。
  - `Definition.aliases`、`ElemDef.deprecated`、`AliasInst` installer (owned_vocab "alias")。
  - `desugar_aliases(def)`: 各 alias を **entry-copy** (canonical name で bind する ElemDef、link 同型)
    または command def に展開。init_sb 前の決定的 pre-pass。option alias は entry-copy を options に、
    command alias は `CommandDef{name:alias, body:clone, export_key:EkKey(canonical)}` を commands に。
  - 再導出規則: **base long** = canonical.long が canonical.name を含む (name 導出 base) なら alias.name
    を採用。**variant** = long_override None なら canonical の name 導出 variant (trigger が canonical
    name を含むもの) を alias name で再導出、明示 variant (gzip 等) は非継承。**short** = alias 自身の
    short のみ (canonical short 非継承)。
  - inst_short が `depr` を ShortEntry へ、inst_long が deprecated 要素へ DeprMark 付与 (経路は用意)。
  - completeness に alias vocab 検査、build_export_map は desugar 済みクローンから収集 (command alias
    の co→checkout 結果キーがマップに乗る)。
- **eval.mbt**: DeprMark 評価 (0-token `@depr:<name>` marker)、`warnings_of(outcome)` (Success の
  `@depr:` marker を canonical 要素名として回収)、`is_depr_key`。
- **matcher.mbt**: walk_short が depr entry 発火時に `@depr:` marker を同梱 (flag/value 両形)。
- **result.mbt / helpers**: `@depr:` を sentinel/marker としてスキップ (結果に出さない)。

### 実測 (phase26_wbtest.mbt、8 テストブロック追加、144 → 152 全 green)

1. **short alias**: canonical port(-p) + `{alias:port, short:n}` → `-n 80` で `{port:80}` (結果キー port
   のみ)、`-p 80 -n 90` → `{port:90}` (同一セルあと勝ち DR-015)。
2. **long alias + variant 再導出 (本命)**: canonical ssl(default true, `--no-ssl` set-false variant) +
   `{alias:ssl, name:tls}` → `--tls`→`{ssl:true}` (base 再導出)、`--no-tls`→`{ssl:false}` (variant 再導出)、
   canonical `--ssl`/`--no-ssl` も不変。
3. **long:[] 明示上書き**: `{alias:ssl, name:tls, long:[]}` → `--tls` は残る (base は name 由来)、`--no-tls`
   は生成されず未定義トークンで Failure。canonical variant は不変。
4. **short 非継承**: canonical ssl(-s) + `{alias:tls, short:t}` → cluster は `s=ssl,t=ssl` (s は一つ、
   継承なら二重生成)。
5. **command alias**: `{alias:checkout, name:co}` → `co --branch x` → `{checkout:{branch:x}}` (co で入るが
   結果キーは checkout、export_key=EkKey(checkout))。
6. **alias installer 順列一致**: AliasInst を先頭/末尾どちらに置いても canon_scope 一致。
7. **deprecated alias (DR-058)**: canonical port + 通常 alias -n + deprecated alias -o → `-o 80` は
   `{port:80}` 受理 + warnings に `port` 積む、`-n`/`-p` は warnings 空。
8. **実体側制約との噛み合い**: `decrypt requires key_file` + alias -d → `-d` 単独で `decrypt@1/constraint`
   (alias 起動でも実体 committed が立ち制約が効く)、`-d --key-file k` → 充足。

## 契約割れ / flag (メインへ)

- **ハード契約割れゼロ**。DR-057 の意味論 (link + 表示帰属、結果キー canonical、name 導出再導出 /
  明示綴り非継承) を忠実に実装。

### flag: 項目 2「variant DSL の name 置換」に曖昧さあり — substring 復元で解決、affix 保持が本筋

**team-lead 指摘どおり曖昧さが出た**。PoC は variant を **パース後の具象 trigger** (`{trigger:"no-ssl"}`)
で保持していて、variant DSL (`no:set:false` の "no" が affix、name は導出時に代入) の **affix 構造を捨てて
いる**。alias name での再導出には affix が要るが、具象 trigger からは「どこが name 部分か」が一意に読めない。

**採った解決**: alias desugarer に局所化した substring 復元。

1. name 導出判定: variant trigger が canonical name を **部分文字列として含む** なら name 導出 (`"no-ssl"`
   ⊃ `"ssl"`)。含まなければ明示 trigger (`"gzip"` ⊅ `"compress"`) → 非継承 (§3)。
2. 再導出: canonical name の **最初の出現**を alias name に置換 (`"no-ssl"` → `"no-tls"`)。

**曖昧さ/脆さ (flag 本体)**:
- 退化ケースで破綻: 1 文字 name ("s") は "no-ssl" に複数出現、name が明示 trigger に偶然含まれる場合も
  誤判定。実用的な複数文字 name が境界に来る場合は well-defined。
- **本筋は affix を明示保持** (DSL `no:set:false` の "no" をデータとして持つ)。`Variant` に `affix` を足すのが
  忠実だが、この MoonBit (0.10.2 nightly) は **struct フィールドのデフォルト値 (`= None`) 非対応** を実機確認
  したため、追加すると他弾の spec に散った 6 個の variant リテラルへ `affix: None` が波及する。波及を避けて
  substring 復元を alias desugarer に閉じ込め、この flag で明示した。**production 実装は affix 保持を推奨**。

### 設計判断 note (曖昧さ無し、観測等価)

- **alias installer は決定的 pre-pass**: DR-042/057 は「global 同型の宣言的コピー、入口展開は入口
  installer が不動点反復」と表現。PoC は AliasInst を apply_node で no-op にし、desugar_aliases を init_sb 前に
  実行 (repeat の init_sb lowering と同型)。理由: PoC は command の子背骨を init_sb 時に構築し不動点で
  children を増やさない。**観測結果は同一・順序非依存** (項目 6 で実証)、entry-copy の衛星展開は long/short/
  command installer が担う (§4 「alias installer は入口形式を知らない」も満たす)。
- **deprecated 警告は `@depr:` marker binding** (Outcome 構造非変更、`@act:` FailMark 同型)。warnings_of が
  Success の marker を回収。Failure は単一勝ち経路が無いため PoC では warnings 非対応 (DR-058 は成功後の
  利用警告なので Success で足りる)。
- **base long 再導出の gate**: 「canonical.long が canonical.name を含む」= name 導出 base の判定。明示追加
  long (name ≠ spelling) は非継承 (§3 明示綴り)。command alias の deprecated は CmdSat に depr が無く未対応
  (項目 5 は非 deprecated なので射程内)。

## 未解決 / 射程外 (第 16 弾で更新)

1. variant affix の明示保持 (flag、本筋は Variant に affix、MoonBit デフォルトフィールド非対応で substring 復元に
   fallback)。
2. deprecated command alias (CmdSat の depr) と Failure 経路の warnings は PoC 射程外。
3. hidden (DR-058) は help/補完除外 = 表示層の関心で、実行時 bundle 非同梱 → PoC 未実装 (DR-058 §1 どおり)。
4. 前弾までの持ち越し (warn 層 lint、完成オラクルのスコープ相対化) は依然未検証。
