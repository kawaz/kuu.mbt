# 2026-07-02 垂直スライス PoC — 評価器コア + installer 連鎖

DR-038/041/042 の契約を「動くコード + 契約を輪郭ごと固定したテスト」で検証する
垂直スライス。製品コードではなく検証装置。場所: `poc/` (MoonBit, `kawaz/slicepoc`)。

toolchain: moon 0.1.20260626 / moonc v0.10.2 nightly。新形式 `moon.mod` / `moon.pkg`
(feature flag `rr_moon_mod,rr_moon_pkg`)。全ソースはルートパッケージ、テストは whitebox
(`*_wbtest.mbt`)。

## アーキテクチャ (三層, DR-039)

1. **ノード ADT** (`node.mbt`): `Exact / StrArg / NumArg / Bind / Or / Seq / Many /
   Scoped(name) / ScopeNode(Scope) / NativeMatch(Matcher) / DdMark`。
   `Scope` は greedy 面 (順不同トリガ) + positional 面 (順序消費) + entities (値セル)。
2. **評価器** (`eval.mbt`): bounded path-search。`eval` が枝 (`Branch = Accept|Held`) を
   全列挙、`parse` が全消費完全経路を数え 0/1/2+ で結末 (DR-038)。
3. **結果**: 現状は flat な `Array[Binding]` (key=value@source)。`Scoped` は path 数に
   透過 (name 駆動ネストは result-builder の軽い課題として据え置き)。

### matcher は「データ」でありクロージャではない (重要な実装判断)

DR-041 の native matcher を**不透明クロージャ**ではなく**データ** (`Matcher` enum:
`EqSplit` / `ShortCombine` + entry 表) として持った。DR-042 の「回収したエントリ表が
matcher の構成データ」と一致し、かつ **AtomicAST 全体が `derive(Eq, Show)` 可能**になる
→ Phase 2 の順序非依存 permutation テストで最終 AST を構造比較できる。クロージャだと
比較不能で permutation 検証が書けなかった。**これは Phase 1 で確定した設計上の勝ち。**

### 先食いの述語 = 「greedy が読むか」(DR-041 §4 の素直な写し)

`greedy_reads(scope, toks, pos)` = 「いずれかの greedy エントリが pos で ≥1 トークン
消費する読みを持つか」。true なら positional の raw 素通し枝を生成しない。DR-041 §4
「次トークンがそのスコープの greedy エントリに読める場合、素通し消費の読みは枝として
生成しない」の逐語的実装。追加のトリガ集合計算は不要だった。

### 完全経路の「重複」= 同一 interpretation として dedup

interleaving 探索は同じ結果に別ルートで到達しうる (例: greedy 発火を Many の 0-rep
前後どちらでも試す)。**完全経路を binding 列の完全一致で dedup** することで false
ambiguous を消す。根拠: DR-038 の「1本」は「1つの解釈」の意味で、binding が同一なら
利用者は区別できない = 1解釈。異なる解釈は異なる binding を生むので dedup は真の
ambiguity を保存する (sort せず完全一致のみ dedup、順序も保存)。

## Phase 1: 評価器コア — 13/13 pass

| 契約 | 根拠 | テスト結果 |
|---|---|---|
| 完全経路の一意性 0/1/2+ | DR-038 | 3 test pass (fail-with-Error / commit / 受け皿ありで ambiguous) |
| 多重 Accept `-n1.0f` | DR-041 §3 / DR-038 例 | ambiguous:2。**contrast も固定**: `-n1.0` は 1解釈、`-f` 未定義なら `-n1.0f` も 1解釈 (曖昧性は定義依存) |
| 先食い (3 面) | DR-041 §4 | positional 間 interject / Many 反復間 interject (多孔質) / greedy 内部 raw (`--color -v`→color=-v) 全 pass |
| prefix ガードなし | DR-041 §5 | 未定義 `-x` → string positional 成功 / number positional は Error 保持 |
| スコープ境界 greedy 断ち | DR-042 ⑤ | 親 greedy が子 scope の positional 消費に割り込まない (子が `--opt` を raw 消費) |
| DESIGN §15.4 `--color 255 0 0` | DR-038 | 可変アリティ + positional 受け皿で ambiguous:2 |

## 契約が割れた/怪しかった箇所

### F-1: dd の `--` マーカーは 先食い の対象外 (DR-041 §4 の文言ギャップ)

DR-041 §4 の 先食い は文言上 **「greedy エントリ」に読めるトークン**だけを raw 素通しから
守る。ところが dd の `--` は**構造要素であって greedy ではない** (DR-041: 「dd は matcher を
持たない」)。

厳密実装すると: dd が optional で継続 positional が寛容 (`Many(StrArg)` 等) の場合、
リテラル `--` が出現すると

- **dd-present 枝**: `--` を消費 → 継続を greedy 断ち子スコープで消費
- **dd-absent 枝**: `--` を positional が raw 消費 (先食いは greedy しか見ないので抑制されない)

の**両方が全消費完成 → ambiguous** になる。つまり `prog -a -- -b` すら曖昧になり、
「`prog -a -- -b` と `prog -- -a -b` で解釈が変わる」という DR-042 dd の狙いが**壊れる**。

**PoC の解決 (2 段階で確定)**:

1. **失敗した第 1 案** (位置局所の特権化): DdMark を Or 的に present/absent 枝で持ち、
   「現トークンが `--` なら present 枝のみ」とした。**これは漏れる**。`prog -a -- -b`
   では -a 発火後に DdMark へ到達する経路 (present, 正) と、DdMark absent 枝が継続を
   親 greedy 込みで inline してから深い位置で `--` を raw 消費する経路が**両立**し、
   Phase 2 の dd テストが `ambiguous:2` になった (`{a,x:-b}` と `{a,b,x:--}`)。
   位置局所の特権化では、absent 枝が `--` をスキップした先で `--` が食われるのを
   止められない。
2. **確定案** (severed モード + `--` を抑制トリガに): scope 走査に `severed : Bool` を
   通す。`--` を消費したら以降 severed=true (greedy 面 off、全て raw)。かつ 先食い
   述語 `suppressed` を「greedy が読む **または** (`token=="--"` かつ scope が dd を
   持ち未 sever)」に拡張。これで `--` はどの深さでも raw 消費されず、absent 枝の漏れが
   消える。DdMark は payload なしの純マーカーに単純化 (継続は後続 positional 要素そのもの)。

結果、解釈変化が素直に通る: `-a -- -b` → `a=true, x=-b` / `-a -b c` → `a=true, b=true, x=c`
(`--` の有無で -b の解釈が反転)。

**メインへ (DR 化候補)**: DR-041 §4 の 先食い を「greedy エントリに読める」だけでなく
**「スコープの構造トリガ (greedy ∪ 未 sever の dd marker `--`)」**に拡張すべき。現状文言
(greedy 限定) のままだと寛容 positional + dd で曖昧を量産する。「dd は matcher を持たない
純構造」(DR-041) は保ったまま、抑制トリガ集合にだけ `--` を載せる形が最小侵襲。

## Phase 2: installer 連鎖 — 8/8 pass (合計 21/21)

`parse_definition` = installer 適用の連鎖。`Definition` (options/positionals の `ElemDef`、
属性 long/short/env/is_dd は mutable = 消費即除去) → 4 installer → `Scope` (AtomicAST)。

| 検証 | 根拠 | 結果 |
|---|---|---|
| 消費即除去 → 冪等 | ① | 4 installer を同一 build に 2 巡 → canon 一致 (再適用は 0 回収 = no-op) |
| 交差禁止 | ③ | 同一語彙を 2 installer が主張 → registration error |
| 完全性検査 | ① | env 属性ありで EnvInst を外す → `unknown special vocab 'env:PORT'` |
| **順序非依存 24 順列** | 5 則 | 全 24 順列で canon_scope 完全一致 (distinct=1) |
| ラダー席宣言 + source | ④ | cli>env>default で source タグ保存 (`@cli`/`@env`/`@default`) |
| 意地悪 1 要素全部盛り | 合成 | long+short+env が name "port" の**単一 entity に合流** (link 合流)、3 形式全て同一セル |
| dd 再スコープ end-to-end | ⑤ | `--` 有無で -b の解釈反転を installer 出力で確認 |
| matcher 4 形式 | DR-041/042 | `--port 8080` / `--port=8080` / `-p8080` / `-p 8080` 全て同一セル |

### 契約が割れた/怪しかった箇所 (Phase 2)

**F-2: dd installer は構造を作らず「語彙の licensing」に縮退した (installer 署名の発見)**。
long/short は具体構造 (exact 衛星 / matcher データ) を build に積む。ところが dd は:
継続 = 後続 positional 要素そのもの、再スコープ = 評価器の severed モード、marker 配置 =
core lowering (`init_build`)。結果 **dd installer の実務は `is_dd` の回収 (= 除去) だけ**に
なった。「installer が再スコープを植え付ける」(DR-042) の実装像は、**installer=語彙所有 /
評価器=意味論 / core=マーカー lowering** の 3 者分担に自然に割れた。これは DR-042 が
「射程外」とした installer シグネチャ・座席の実測データ。long/short (構造構築型) と
dd (語彙 licensing 型) は**別カテゴリの installer** と見なすのが素直。

### matcher の座席 (task 3 の問い) — 実測回答

native matcher は **greedy 面のエントリ (scope.greedy 配列中の 1 Node = `NativeMatch`)**
として落ちた。filter chain の席でも専用席でもなく、exact 衛星と**同列の greedy 市民**。
根拠: 先食い述語 `greedy_reads` が「greedy エントリが読むか」で matcher も exact 衛星も
一様に扱えたため、両者を同じ配列に混ぜるのが最も素直だった。

副次発見: short の「単一文字 `-v` / `-p 8080`」も ShortCombine matcher が被覆するため、
**short installer は別途 exact 衛星を積まず matcher 1 個だけ**になった (DR-042 の「greedy
衛星 + matcher」のうち衛星が matcher に吸収)。long は `--k=v` (eq-split matcher) と
`--k v` (exact 衛星) が別機構なので**両方**積む。short と long で衛星の要否が非対称。

### installer シグネチャ (自然に落ちた形)

`(Definition, Build) -> Unit`。Definition から所有語彙を回収 (mutable 属性を空に) し、
Build (greedy: 追加専用 / entities: env_key を in-place 宣言 / positional: core 所有) に
寄与。値源系 (env) は entity の席 (`env_key`) を宣言するのみ (ラダー順は評価器所有、
不変則④)。順序非依存は「greedy は追加専用・entities は名前で in-place・positional は
dd 以外触らない」から出た。中央 gather-then-build は不要だった (DR-042 の判断を追認)。

## 未解決 (メインが DR 化を検討すべき)

1. **先食いの構造トリガ拡張** (F-1): 上記「メインへ」。最優先。
2. **installer の 2 カテゴリ** (F-2): 構造構築型 (long/short) と語彙 licensing 型 (dd) を
   DR-042 の installer 定義で区別するか。native matcher の座席は「greedy 面エントリ」で
   確定してよいか。
3. **完全経路の dedup 規約** (Phase 1): 「同一 binding 列 = 1 解釈」を DR-038 の
   「1本」の正式定義に格上げするか (interleaving 実装が別ルート重複を出すため実装契約として要る)。
4. **name 駆動 result builder** (layer 3): 本 PoC は flat binding に留めた。`Scoped` の
   ネスト result 構築は未実装 (path 数には無関係なので Phase 1/2 契約検証には不要だった)。

## commit（第 1 弾）

- Phase 1: `poc/` 評価器コア (value/node/eval/matcher + helpers/phase1 test) + 本 journal
- Phase 2: installer 連鎖 (installer.mbt + phase2 test) + eval.mbt の dd severed モード改修
  + 本 journal 追記

---

# 第 2 弾: spec 改稿 (DR-041 背骨 / DR-042 非削除①' / DR-043 repeat) 追従

第 1 弾の F-1/F-2 を受けて仕様が更新された。追従して足場を揃えつつ command/global/
inherit/repeat/multiple まで拡張する。

## Task 1: dd を greedy exact 衛星に再 encode（先食い特例の撤回）

第 1 弾では F-1 として「先食いを (greedy ∪ dd マーカー) に拡張すべき」と報告したが、
**DR-041 改稿はこの特例を却下**し、dd を **greedy 面の住人 (トリガ兼消費者)** と捉え直した。
`--` が値に食われないのは「dd が greedy エントリだから 先食いが効く」、以降の option 抑制は
「発火した greedy の内部消費は一体」から導出 — **dd 専用規則ゼロ**。

実装追従:
- `DdMark`（positional 純マーカー）→ `DdSat(String)`（greedy 面の exact 衛星）。
- `suppressed` を **greedy-only** に戻す（`scope_has_dd` / `token=="--"` 特例を削除）。
  `--` の抑制は「DdSat が greedy エントリとして `--` を読む」→ `greedy_reads` が true から出る。
- greedy 発火ステップで **DdSat が発火したら severed=true で継続**（内部一体則の実装）。
  `severed` フラグは DR-041 が明示的に許容する encoding（「経路局所の sever フラグ + 純マーカー」）。
- core lowering: dd positional は**背骨に載せない**（inst_dd が DdSat を greedy に追加）。

**確認**: 既存 dd テスト（`-a -- -b`→`a,x=-b` / `-a -b c`→`a,b,x=c`）が**特例なしで通る**
（21/21 維持）。第 1 弾 F-1 の「特例が要る」は spec 改稿で解消 — dd=greedy 住人の捉え直しが
正しく、第 1 弾の特例は不要だったことを実測で確認。add-if-absent (`add_greedy`) も先行導入。

## Task 2: installer プロトコルを非削除型へ (DR-042 不変則①')

**消費即除去 → 宣言層 read-only + add-if-absent** に変更。`ElemDef` の属性は `mut` を外し、
installer は語彙を**読むだけ**。冪等性の出所が「2 巡目は空だから no-op」から
「2 巡目も再寄与するが add-if-absent が弾く」に移った。完全性検査は「消費後の残余」では
成立しなくなる（非削除だと常に残る）ため、**「使われている語彙 ∉ 所有集合」の検出**
(`unowned_vocab`) に置換。`order` が所有集合を決める。全体を**不動点反復**で回す
(`build_sig` を巡回前後で比較、変化なしで停止)。global 不在なら 2 巡で収束 (寄与→no-op)。

**割れ/怪しさ**: なし。非削除化は素直に通り 22/22。冪等の機構が入れ替わっただけで観測挙動不変。
副産物として「宣言層が inert に残る = def0 が不変 = 再パースで同一 AST」が
テストで固定でき、help/diagnose/再シリアライズの前提 (invariant ①') が実測で裏付いた。

## Task 3: command / global / inherit + 木構造 scope builder

parse_definition を**フラット 1 scope → scope builder の木**へ再構築。`Definition` に
`commands: [CommandDef]` を追加、`SB`(scope builder) が decl 層 (global コピーで options が
成長) + build 層 (add-if-absent で成長) + children を持つ。不動点は木全体を 1 pass する
`tree_pass` を `tree_sig` 安定まで回す。materialize が SB 木 → Scope 木 (bottom-up)。

### 契約が割れた/怪しかった箇所

**F-3 (最重要): command を「greedy トリガ + 新背骨」で素直に encode すると、子 scope の
短い受理 (0 消費) が親 greedy への fall-back を生み false ambiguous になる。命令衛星は
発火時に親を sever する必要がある (dd と同型)。**

DR-042 表 /タスク指示は「command = トリガは greedy exact、部分木は新背骨」。最初これを
greedy エントリ `Seq([Exact(name), ScopeNode(child)])` で写した。ところが
`root(global --verbose) → build(--verbose)` に `build --verbose` を与えると:

- 経路 A: build に入り、child の `--verbose` (lverb) が発火 → `{lverb}` (pos=2 完全消費)
- 経路 B: build に入るが child が **0 消費で受理** (child の positional は空なので greedy を
  撃たず即 accept)、親 scope_step に pos=1 で復帰 → **親の greedy `--verbose` (gverb) が発火**
  → `{gverb}` (pos=2 完全消費)

両方完全経路 → `ambiguous:2`。lexical 解決 (最小 scope 優先, DR-032/033) の意図は「内側の
build が勝つ」なのに、親 greedy の復帰がそれを壊す。

**解決**: 専用ノード `CmdSat(name, Scope)` を追加。dd と同じく **発火時 severed=true で親を
断つ**。「subcommand は自分より後ろを全部所有する = 発火した greedy の内部消費は一体」
(DR-041 §4) の直接の帰結。これで経路 B は「親 severed + 親 positional なし + `--verbose`
未消費」で受理できず消え、`build --verbose` → `{lverb=true}` に一意化。

つまり **DR-041/042 の「命令部分木は新背骨」だけでは足りず、「発火した命令は親 greedy を
sever する」を明示すべき**。dd の「内部一体」則を command にも適用するのが自然な補完
(dd と command はどちらも「greedy 発火 → 以降を内部消費として引き取る」同型)。DR-018 の
「positional 面の排他 or」像とは別経路だが、観測挙動 (commands が先 = 先食い、subcommand が
後続を所有 = sever) は等価に出る。

**F-4: DR-018 の「positional 面 or 展開」と DR-041/042 の「greedy トリガ + 新背骨」は
別の構造像。PoC は後者 (greedy 面 CmdSat) を採った。** 「commands が先」の優先は先食い
(command 名を positional の raw 食いから守る) で創発し、「素の positional との排他」は
CmdSat の sever が担う。DR-018 の or 構造 (元 positional をまとめて or 枝にする) は採らず、
元 positional は positional 面に残したまま command を greedy 面に置く。DR-018 (先行) と
DR-041/042 (後発) で command の構造像が食い違う箇所であり、後発を採るのが現行仕様と整合。

**F-5: global の shadowing は「同名トリガ」判定で、名前判定では不足。** 当初 name 一致で
コピー抑止したが、`親 global(name=gverb, --verbose)` と `子(name=lverb, --verbose)` の
ように**別名・同トリガ**だと name 判定は抑止に失敗しコピーが漏れて ambiguous 化する。
DR の「同名トリガを自前宣言するスコープへはコピーしない」を素直に読むと **trigger 集合の
重なり**で判定すべき (`triggers_overlap`)。修正後、`build --verbose` は子の lverb が一意に勝つ。

**確認済み契約**:
- (a) 非削除 + 不動点で順序非依存: **7 installer 全 5040 順列で canon 木が distinct=1**。
  global × long/short (子 scope への global コピー → 子の long/short が衛星化) が order に
  依らず収束することを含む。global が order の末尾でも、tree_pass が「apply 全 installer →
  子へ再帰」の順で子は同 pass 内で成長後 decl を見る + 外側不動点で二重に保証。
- (b) 背骨規則の一意性: command 新背骨 = CmdSat が child を eval_scope (fresh, unsevered)、
  親 greedy は CmdSat 発火で sever。dd 継続・greedy 内部は背骨なし (既存)。その他は現在の
  背骨 (既存)。**一意に実装できたが、command について「sever する」の 1 点を仕様に足す
  必要があった (F-3)**。
- inherit ラダー: cli > env > inherit > default を end-to-end で確認 (親 cli=50 → 子 inherit=50、
  親 default=30 → 子 inherit=30、子 cli=7 は inherit に勝つ、祖先なしは子 default=99)。

### installer シグネチャ・不動点の自然な形

- 構造寄与型 (long/short/dd/command): build 層 (greedy) へ add-if-absent。
- 席宣言型 (env/inherit): entity の席 (env_key / inherit フラグ) を in-place 宣言。
- 宣言コピー型 (global): **子孫の decl 層** に add-if-absent コピー (invariant ②の唯一の
  「宣言層への追加」)。これだけが後続の回収対象になる = 不動点が要る唯一の源。
- command は materialize 時に CmdSat 配線 (決定的)。所有語彙 (completeness) だけ order 依存。
- 不動点は「decl 層の成長 (global) ∪ build 層の成長 (add-if-absent)」が止まるまで。停止性は
  「要素 × scope 有限 + コピーのコピーは同トリガで add-if-absent が止める」から。
